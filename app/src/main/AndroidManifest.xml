<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="projetosestudos.felipe.com.trabalhe_conosco_mobile_dev">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.CadastroActivity"
            android:theme="@style/AppCadastro" />
        <activity
            android:name=".activity.LogarActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.AdicionarCartaoActivity"
            android:theme="@style/AppAddCard" />
        <activity android:name=".activity.AddMoneyActivity"
            android:theme="@style/AppAddCard"/>
        <activity android:name=".activity.PaymentActivity"
            android:theme="@style/AppAddCard"></activity>
    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.picpay.andbar.testepicpay">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:background="@android:color/holo_green_dark">
        <activity android:name=".TestePicPayPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Transferencia"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Transferência de Valores">
        </activity>

        <activity android:name=".CadastraCartao"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Cadastre seu Cartão de Crédito">
        </activity>

    </application>

</manifest>